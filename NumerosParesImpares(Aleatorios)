#include <stdio.h>
#include <stdlib.h>

int main()
{   //Refazer o exercício anterior
    //mas a quantidade de números a serem lidos é desconhecida. Finalize o programa quando ler o número 9999

    int numero;

    while
    for
       if(numero > 0) {
       printf("eh positivo");
    } else {
       printf("eh negativo");


    int numero2;

    while
    for
       if(numero2 = 0) {
       printf("eh nulo");
    } else {
       printf("nao eh nulo");
    }



        int n =1, p=0,i=0,a;


    while (n <= 10){
        n = n + 1;

    if (a % 2 == 0){
                a = p;
                p = p + 1;
            }else{
                a = i;
                i = i + 1;
            }

            }
    }

    if (n == 9999){
      printf("\n Saindo do loop\n");
      break;
    }

    printf("A qtd de números pares é: ", p);
    printf("A qtd de números ímpares é: ", i);


    printf("Hello world!\n");
    return 0;
}
